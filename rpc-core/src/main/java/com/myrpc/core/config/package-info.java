/**
 * 配置层：框架运行时配置类。
 *
 * <p>本包定义：
 * <ul>
 *   <li>{@code RpcServerConfig} —— 服务端配置（端口、注册中心地址等）</li>
 *   <li>{@code RpcClientConfig} —— 客户端配置（注册中心地址、直连地址、负载均衡策略等）</li>
 * </ul>
 */
package com.myrpc.core.config;
